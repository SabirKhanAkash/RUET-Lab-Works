close all;
clear all;
clc;

    c_F1 = 11;
    N = 1/100;
    
fs = 100;
sampling_step = 1/fs;
StopTime = 1;
c_t = (0:sampling_step:StopTime)';
x1 = @(t) exp(j*2pi*C_F1*t)+.5;
y1 = x1(c_t);
figure(1);
plot(c_t,y1,'bo-','LineWidth',.5);
pause(2);

figure(2);
ax1 = subplot(2,1,1);
ax2 = subplot(2,1,2);
hold(ax2,'on');
for D_F = 0:.1:C_F1+3
    x = @(t)exp